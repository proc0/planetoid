// field starfield.OffsetBg

pub fn starfield.Starfield {
    use body=starfield
    use radius=BOUNDS_RADIUS
    
    Body(
        pos=@(0, 0),
    )

    repeat 15 {
        Spawn star { Star }
    }

    with Tick(3s) {
        Spawn star { Star(pos=RandomSpawnTopPos(2)) }
    }

}