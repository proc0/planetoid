Play(duration=0.8s) {
    Vibrato(freqModulation=1600) {
        WhiteNoise
    }
    Oscillator(wave=$triangle, freq=40)
    LowPass(freq=[800,0])
    HighPass(freq=40)
    Envelope(attack=0.02s, sustain=0.5s, decay=0.07s, decayTo=0.6)
    Gain(volume=0.7)
}