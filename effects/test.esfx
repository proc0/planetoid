Play(duration=5s) {
    Vibrato(freqModulation=50) {
        WhiteNoise
    }
    BandPass(freq=[120,30], q=[1,5])
    // Oscillator(wave=$sine, freq=100)
    BrownNoise
    LowPass(freq=80)
    // Envelope(attack=0.25s, release=0.5s)
    Gain(volume=0.1)
}

// Play {
//     Oscillator(freq=500)
//     Envelope(attack=0.01s)
//     Output(into=$abc)
    
//     Oscillator(freq=750)
//     Envelope(attack=0.5s)
//     Output(into=$abc)
    
//     Input(key=$abc)
//     LowPass(freq=1000) // receives from the two Outputs above
// }